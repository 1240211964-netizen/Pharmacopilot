services:
  - type: web
    name: pharmacopilot
    runtime: node
    plan: free
    buildCommand: "npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: "22"
