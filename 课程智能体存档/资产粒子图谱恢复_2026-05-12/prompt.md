# Prompt

User requested implementation of the approved plan: keep the current teaching assets workbench, but restore the previous canvas-based particle knowledge graph interaction.

Hard requirements:
- Preserve the current workbench, filters, asset manual, and reuse action band.
- Restore the canvas particle graph from the pre-workbench version.
- Keep bidirectional linking: clicking a graph asset node updates graph detail and asset manual; clicking an asset card highlights the graph node.
- Do not add React/Tailwind or backend routes.
- Archive before and after code, checks, prompt, and change log, then commit and push.
