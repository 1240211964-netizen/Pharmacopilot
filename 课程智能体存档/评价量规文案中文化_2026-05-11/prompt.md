# 评价量规文案中文化计划

## 用户要求
不要再出现 rubric 的字样了，全部替换成中文。

## 执行范围
- 运行页面、按钮、卡片、占位符、生成结果提示、后端 fallback 文案中，不再出现该英文评价术语。
- 内部实现名、API 路径、数据字段、CSS class、函数名暂不重命名，避免破坏现有接口、训练状态机和前端绑定。
- 本轮继续先留痕、后改造、再检查、提交并推送到 origin/main。
