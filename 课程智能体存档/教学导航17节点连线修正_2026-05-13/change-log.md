## 修改范围

- `前端核心/app.js`
  - 修正 `routeMetroLineDefinitions` 中 `review` 线路的 SVG path。
  - 原路径从节点 13 坐标 `M 43 68` 起笔，视觉上形成 `13 -> 17` 的错误连接。
  - 新路径从节点 16 坐标 `M 9 68` 起笔，再连接到节点 17–20。

## 设计判断

- 13–16 属于课中阶段的“评价证据”线路。
- 17–20 属于课后阶段的“复盘改进”线路。
- 教学流程语义应为 16 后进入 17，而不是从 13 分叉到 17。
