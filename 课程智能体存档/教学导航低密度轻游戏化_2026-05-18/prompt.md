# Prompt

用户要求将 `teaching-navigation` 页从“信息丰富版”改成“低密度轻游戏化版”：

- 页面不要太满，保留闯关感。
- 关卡节点默认只显示编号、状态/能力图标和短标题。
- 右侧详情默认只显示关卡编号、标题、本关任务、通关条件和主操作。
- 训练态默认只显示 3 个推荐方案，其他内容折叠。
- 复盘区压缩为底部 summary strip。
- 不改变 20 环节数据、三阶段结构、localStorage、通关逻辑和当前关卡推进逻辑。

