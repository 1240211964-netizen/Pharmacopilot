# 修改日志

## 计划

- 归档修改前 `前端核心/navigation.html`、`前端核心/app.js`、`前端核心/styles.css`。
- 记录修改前构建检查。
- 参考首页 `index.html` 的浅色纸面、细边框、功能卡片、紧凑 chip 风格。
- 将训练地图从游戏浮岛视觉收敛为首页同款产品化路径卡片。
- 保留节点点击、任务单浮层、方案选择、确认关卡、锁定提示等交互。
- 记录修改后检查与浏览器检查，提交并推送 GitHub。

## 结果

- 已将“20 环节教学能力地图”从游戏浮岛/路线装饰改为首页同源的浅色纸面路径布局。
- 节点改为默认展示编号、环节名称、状态和评分标签，避免只有当前节点才显示标题。
- 地图保留左侧进度状态栏，主区域按 3 个真实教学阶段组织：课前教学设计与准备、课中教学实施与调控、课后评价反馈与持续改进。
- 移除了不匹配当前数据结构的第 4 个阶段图例，避免布局空位和语义不一致。
- 地图内任务单交互保持不变：点击可进入节点打开任务单，选择 A-F 方案后刷新选择态，关闭按钮可关闭任务单。
- 留痕已保存修改前后代码、prompt、修改日志、before/after 构建检查和浏览器检查。

## 检查

- `node --check 前端核心/app.js`：通过。
- `npm run build:static`：通过。
- `npm run build:server`：通过。
- `navigation.html` 浏览器检查：3 阶段、20 节点、无右侧常驻卡片、旧路线装饰隐藏、任务单交互、移动端单列与无横向溢出均通过。
