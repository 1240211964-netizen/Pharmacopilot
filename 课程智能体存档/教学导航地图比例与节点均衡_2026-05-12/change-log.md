# Change Log

## Planned
- Rebalance the teaching-navigation route node coordinates across the map canvas.
- Keep the map dominant after opening a node detail card.
- Preserve the existing explanation-first and training-mode workflow.

## Completed
- Repositioned the 20 route nodes into a more balanced three-stage S-shaped journey across the map canvas.
- Updated stage island geometry to match the new node distribution.
- Moved the opened node detail card out of the left-side dominant position and constrained it to a narrower right-side auxiliary panel.
- Increased map canvas height on desktop so the map remains the primary visual area.
- Shortened visible selected/current node labels to compact step names while retaining full titles in the accessible label and tooltip.
- Updated teaching-navigation cache query strings for `app.js` and `styles.css`.
