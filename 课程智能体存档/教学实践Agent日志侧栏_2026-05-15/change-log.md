# Change Log

- 在 `practice.html` 的 20 环节路线区域新增右侧 `agent-state-panel`，展示“智能体运行态”、任务上下文、运行中状态、5 条日志和案例匹配进度。
- 将原 20 环节节点和教师介入说明包进 `practice-route-main`，与 Agent 日志组成 `practice-route-layout` 双栏结构。
- 为 Agent 日志侧栏新增深色日志卡样式、状态标记、进度条、桌面 sticky 行为和移动端单列响应式适配。
- 保留现有 20 个环节、STEP 12 当前运行、STEP 14 / 17 / 20 教师介入提示和下方 4 个指标。
