# 修改日志

## 计划

- 归档改造前 `前端核心/navigation.html`、`前端核心/app.js`、`前端核心/styles.css`。
- 记录改造前检查结果。
- 将地图板块从“左侧状态 + 中间地图 + 右侧详情”改为“左侧状态 + 主地图”。
- 把关卡详情改成地图内任务单浮层，支持打开、关闭、Esc 关闭、方案选择、确认关卡和进入完整训练工作台。
- 移除该板块中 `trainingMapAssets` 的可见渲染。
- 记录改造后检查、浏览器检查，并提交推送 GitHub。

## 结果

- 已移除“20 环节教学能力地图”右侧常驻关卡详情和已解锁资产卡片。
- 已把 `trainingMapDetailPanel` 与 `trainingMapStepDetail` 迁移为地图内部任务单浮层，默认隐藏。
- 点击已完成、当前、可进入节点会打开浮层任务单；点击锁定节点只显示锁定提示。
- 任务单包含节点编号、状态、环节标题、核心问题、教师任务、产出物、评价维度、A-F 方案、系统提示、确认本关卡、进入完整训练工作台和关闭按钮。
- 选择方案后浮层保持打开并刷新选中状态；确认本关卡后地图进度更新并关闭浮层；Esc 和关闭按钮均可关闭浮层。
- 已完成桌面、390px 移动端、900px 平板宽度浏览器检查。
- 改造后检查均通过：`node --check 前端核心/app.js`、`npm run build:static`、`npm run build:server`。
