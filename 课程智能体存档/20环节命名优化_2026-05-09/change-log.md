# 修改日志

## 计划

- 归档修改前 `前端核心/app.js` 与相关页面文案。
- 将当前 20 环节重构为“课前教学设计与准备 / 课中教学实施与调控 / 课后评价反馈与持续改进”三段真实教学流程。
- 同步每个环节的 `coreQuestion`、选项、Rubric 主题、Rubric 细分维度和最终实践方案章节标题。
- 保留现有状态机、HTML id、路由与资产逻辑，不新增页面或后端接口。
- 运行 `node --check`、`npm run build:static`、`npm run build:server`。
- 归档修改后代码与检查结果，提交并推送 GitHub。

## 结果

- 已将 20 个教学环节统一为三段高校教学流程：课前教学设计与准备、课中教学实施与调控、课后评价反馈与持续改进。
- 已采用“学情分析与学习起点诊断”替代旧的“学习者特征/学习者分析”表达，并同步最终诊断维度、训练地图、实践方案章节和导航页文案。
- 已更新每个环节的 `coreQuestion`、选项、路由节点任务、Rubric 主题与 Rubric 细分评价维度，使描述更贴近中国高校药学管理课程语境。
- 已保留现有 HTML id、状态机、路由和后端接口，不新增独立页面或接口。
- 修改后检查均通过：`node --check 前端核心/app.js`、`npm run build:static`、`npm run build:server`。
- 已用本地静态服务检查 `navigation.html` 和 `practice.html`，确认新版术语可见且旧词 `学生画像`、`学习者特征` 未残留在受检页面。
- 已归档修改前后代码、prompt、修改日志和检查记录。
