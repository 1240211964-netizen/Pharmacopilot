# Browser / Page Checks

- 本轮为首页主标题文案调整，不涉及布局和交互逻辑。
- 已执行 `npm run build:static`，并在构建后扫描 `前端核心/index.html`。
- 检查确认新 slogan “面向新教学范式，重构新教师备课路径。” 存在。
- 检查确认旧 slogan “把药学管理课程备课任务交给教学智能体，回来审校可交付成果。” 不再出现在 `前端核心/index.html`。
