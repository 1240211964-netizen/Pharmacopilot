# 本轮用户请求

请基于我给你的这个 navigation_focus_v3 zip的前台减法版继续优化。

不要把后台说明重新放回主界面。
不要恢复质量雷达、证据链、Agent 运行状态、资产货架等常驻模块。
contract 文件可以保留完整结构，但 UI 只取当前任务必要字段。
每个任务站只让用户完成一个教学判断。
主交互必须是：证据图 → 判断题 → 反馈 → 产物 → 保存资产。
what / why / how 只能折叠展示，不得作为主界面大卡片。
药事情境只能轻量切换，不要做成四张大卡。
不要把页面做成 dashboard。

# 中途更正

你得根据navigation_focus_v3这个文件夹中的修改
