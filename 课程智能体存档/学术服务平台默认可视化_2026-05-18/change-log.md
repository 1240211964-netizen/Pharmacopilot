# Change Log

- 将学术服务平台面板运行时移动到教学数据页 hero 后、教学数据图谱前，使用户打开页面后更快看到内嵌结果。
- 默认填入示例闻道域名 `nju.libsp.net`，并保留可编辑域名输入。
- 将 Word 文档中的 `agentId`、`modelId`、`datasetList`、`exploreId`、`image_ids`、`file_ids`、`threadId`、`promptId`、`retrievalSources` 等参数写入表单和 URL 生成逻辑。
- 新增“厚道对话页面”入口。
- 新增本地可视化预览层：即使真实平台 iframe 被登录、跨域或白名单限制阻挡，页面打开后也能直接展示按 Word 参数拼出的开放访问结果。
