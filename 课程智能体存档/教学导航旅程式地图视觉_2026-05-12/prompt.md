# Prompt

用户要求实施“教学导航旅程式地图视觉改造计划”：

- 目标页：`前端核心/teaching-navigation.html` 的 20 环节教学训练地图。
- 学习用户提供的 React 原型，将当前偏重的深色关卡地图改成“轻背景 + 主路线 + 阶段 pill + 右侧节点说明卡”的旅程式教学地图。
- 保留现有 20 环节数据、三阶段高校教学术语、节点说明态 -> 开始训练 -> A-F/生成/诊断/确认 的交互链路。
- 不引入 React/Tailwind，不新增后端接口，不改旧 `navigation.html`。
- 先留痕，完成后运行检查并提交推送到 `origin main`。
