# 本次需求

用户通过截图澄清：目标不是只嵌入单个 AI 知识库页面，而是做成类似闻道界面的“左侧导航栏 + 右侧嵌入页面”结构，并根据 Word 文件中的指引把所有可嵌入功能都接入。

执行目标：

- 左侧做闻道式功能导航。
- 右侧 iframe 展示当前选中的闻道 openAccess 页面。
- 覆盖 Word 文件中列出的 openAccess 页面类型。
- 保留新窗口打开兜底，不绕过 iframe 安全限制。
- 不改变 PharmacoPilot 现有外层页面结构。
