# Browser / Visible Text Checks

- 本轮为全局术语替换，不改变布局结构或交互流程。
- 已执行活跃源码扫描：`rg -n "药学管理" . --glob '!课程智能体存档/**' --glob '!dist/**' --glob '!node_modules/**'`。
- 结果：当前活跃源码范围内未发现“药学管理”。
- 已执行 `npm run build:static`，构建输出会同步新的“药事管理”口径。
