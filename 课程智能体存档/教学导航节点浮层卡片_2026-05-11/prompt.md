# 用户请求

用户要求实施《教学导航节点卡片浮层改造计划》：将教学导航页当前左侧常驻节点详情栏，改为点击地图节点后出现的地图内浮层卡片；卡片默认摘要态，点击展开后显示 A-F 方案选择、评分提示和完整操作区。

# 约束

- 目标页：`前端核心/teaching-navigation.html`。
- 不改 20 环节数据结构。
- 不新增后端接口。
- 不改旧 `navigation.html`。
- 保留 `teachingNodeDetail` id。
- 本轮先留痕、再修改、再检查、再提交推送 GitHub。
