# Browser / Page Checks

- 使用 `npm run build:static` 后，通过本地静态服务访问 `http://127.0.0.1:4179/index.html`。
- `curl` 检查页面源码，确认不再出现 `.home-proof-row`。
- `curl` 检查页面源码，确认不再出现“教学环节诊断检查点”“课前、课中、课后阶段”“评价证据与教师审校”。
- 同时确认首页主标题、主 CTA、次 CTA 和 `.home-agent-workbench` 仍存在。
