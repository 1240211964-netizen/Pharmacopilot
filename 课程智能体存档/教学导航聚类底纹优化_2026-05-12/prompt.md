# Prompt

用户要求实施“教学导航地图低噪音聚类底纹优化计划”：

- 目标页：`前端核心/teaching-navigation.html` 的 20 环节地图。
- 本轮只优化底层视觉噪音，保留当前 20 个节点坐标、主路线、三阶段叙事、阶段 pill、节点说明态和训练流程。
- 单细胞测序聚类图只作为视觉参考，学习 UMAP/t-SNE 的轻量点云、低透明密度云、柔和聚类边界。
- 不改 20 环节数据结构，不新增后端接口，不改旧 `navigation.html`。
- 完成后提交并推送到 `origin main`。
