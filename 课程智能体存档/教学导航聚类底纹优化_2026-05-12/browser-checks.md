# Browser Checks

本轮通过本地静态服务检查：`http://127.0.0.1:4178/teaching-navigation.html`。

## Results

- 页面标题为 `Pharmacopilot | 教学导航`。
- 20 个节点正常渲染。
- 3 个阶段 pill 正常渲染。
- 3 个阶段聚类底纹区正常渲染。
- 点击阶段二 pill 后，可定位到节点 09 并打开说明态。
- 点击节点 01 后，默认显示说明态，包含“为什么存在 / 需要输入什么”，且不直接显示方案选择区。
- 点击“开始本环节训练”后，显示方案选择区与智能生成草稿。
- 视觉检查确认点云底纹在底层，主路线和节点在上层。

## Notes

- 内置浏览器不能直接访问 `file://`，因此使用本地 HTTP 静态服务检查同一工作区文件。
- 本轮留痕以 before/after 代码、prompt、变更日志和检查记录为准，不依赖截图。
