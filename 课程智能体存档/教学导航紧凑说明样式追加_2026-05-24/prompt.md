# 触发请求

用户要求将指定的 `/* === Pharmacopilot · Compact teaching navigation brief === */` CSS 代码追加到 `前端核心/teaching-navigation-productized.css` 末尾。

要求：
- 如果文件中已存在旧的顶部 hero、课程卡、阶段卡片、环节地图样式，不删除全局样式。
- 只让新追加的 `.pp-*` class 生效。
