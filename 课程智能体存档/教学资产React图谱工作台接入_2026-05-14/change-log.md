# Change Log

## Before
- 已存档教学资产页相关源码，等待改造。

## After
- 将教学资产页主体替换为“管理学原理课程图谱”工作台，保留原有顶部导航和路由入口。
- 新增课程模块图谱节点、SVG 连线、模块筛选、节点点击同步四周面板。
- 中心主干固定为“管理学原理课程图谱”；SWOT 仅作为“计划与决策”模块下的示例节点。
- 四周面板同步显示当前节点概览、模块边界说明、课程资产统计 / 质量诊断、建议动作 / 快捷操作。
- 新增移动端布局约束，390px 下 `documentElement.scrollWidth` 与 `innerWidth` 均为 390。
- 调整根 `tsconfig.json`，排除 `.next` 生成物、历史存档和旁路 API 目录，避免与当前静态页无关的 TypeScript 旧噪声进入根 typecheck。
