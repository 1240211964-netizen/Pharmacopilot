# Change Log

- 创建本轮留痕目录并保存改造前关键文件。
- 将教学导航节点浮层从“摘要 / 展开”改为“说明 / 训练”两段式流程。
- 节点点击默认显示“为什么存在 / 需要输入什么 / 生成什么 / 如何合格”，不再直接展示 A-F 方案。
- “开始本环节训练”后在同一右侧卡片中展示 A-F 方案、智能生成草稿、评分诊断和确认按钮。
- 未选择主方案时禁用“确认本环节”；选择方案只刷新草稿和诊断，不提前推进地图状态。
- 确认本环节后更新节点状态、完成数和当前节点，并在下一节点说明态展示推荐下一步。
- 锁定节点允许查看说明，但训练入口置灰并提示需先完成前序节点。
- 补充说明态、训练态、生成草稿、锁定提示和禁用按钮的样式。
- 完成构建检查和浏览器桌面/移动端交互检查。
