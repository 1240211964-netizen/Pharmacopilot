# Prompt

用户要求实现教学导航节点训练流程改造：

用户点击节点后，右侧卡片先解释这个环节为什么存在、需要输入什么、生成什么、如何合格；用户点击“开始本环节训练”后，才进入 A-F 方案选择、智能生成、评分诊断；用户确认后，节点状态更新并推荐下一步。

约束：不改 20 环节数据结构，不新增后端接口，不改旧 `navigation.html`，训练内容继续在同一张地图右侧浮层卡片内完成。
