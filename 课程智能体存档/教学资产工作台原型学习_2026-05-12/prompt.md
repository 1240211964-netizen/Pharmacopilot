# Prompt

User requested implementation of the approved plan: learn from the supplied React prototype and rebuild the existing static `教学资产` page as a teaching assets workbench.

Hard requirements:
- Do not introduce React/Tailwind or new backend routes.
- Preserve existing key HTML ids such as `assetKnowledgeCanvas`, `assetUploadForm`, `assetList`, and `assetDetail`.
- Replace the decorative particle graph with an explainable relation graph centered on the selected asset.
- Make the upload form collapsed by default under a "导入课程材料" panel.
- Add asset filters for source, type, status, and keyword search.
- Convert asset details into an "资产说明书" preview with source boundary and reuse actions.
- Add a bottom reuse action area for next lesson design, classroom activity, task sheet, rubric, and teaching review.
- Archive before and after code, checks, prompt, and change log, then commit and push to GitHub.
