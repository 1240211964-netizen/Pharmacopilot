# Prompt

User feedback: the current asset relation graph is no longer interactive like the previous star graph, and nodes should be clickable.

Implementation intent:
- Keep the explainable relation graph direction.
- Restore interaction: center asset and surrounding relation nodes must be clickable and keyboard accessible.
- Clicking a graph node should visibly select/highlight that node and update the right-side relation detail.
- Keep the existing asset card/detail behavior intact.
- Archive before and after code, checks, prompt, and change log, then commit and push.
