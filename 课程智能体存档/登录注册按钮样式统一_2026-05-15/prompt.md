# Prompt

用户反馈：index 页和其他页的登录入口视觉不一致，应当以 index 页为准。

截图表现：
- index 页：`登录` 是白底描边圆角按钮，`注册` 是棕色实心圆角按钮。
- 其他页：`登录` 显示得更像普通文本/下划线链接，和 index 页不一致。

本次目标：
- 统一所有页面导航栏右侧未登录状态的 `登录 / 注册` 样式。
- 以 index 页现有视觉为标准。
- 保持现有 HTML 结构与账号状态逻辑不变。
