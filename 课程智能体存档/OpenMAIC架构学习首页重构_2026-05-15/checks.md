# Checks

- `node --check 前端核心/app.js`：通过。
- `npm run build:server`：通过。
- `npm run build:static`：通过。
- `git diff --check` on changed files：通过。
- `npm run build`：通过。
- Local server：`PORT=5180 npm start`，运行于 `http://localhost:5180`。
- API smoke：`POST /api/generate/pipeline` 返回：
  - `ok: true`
  - `pipeline: pharmacopilot-course-generation`
  - `scenes: 5`
  - `evidenceRules: 4`
  - `assetPack.items: 6`
  - renderers: `DesignSceneRenderer / PracticeSceneRenderer / EvaluationSceneRenderer / AssetGraphRenderer / ReflectionSceneRenderer`
- HTTP smoke：`index.html` 和 `practice.html` 可返回新导航文案。
- Browser smoke：
  - desktop 首页标题为 `从课程任务生成可运行教学场景。`
  - nav 为 `课程生成 / 实践运行 / 教学资产 / 评价证据`
  - actions 为 `查看样例 / 进入工作台`
  - tabs 为 `课程生成 / 实践运行 / 教学资产 / 评价证据`
  - homepage evidence mode submit 成功写入 `pharmacopilot-home-pipeline-result`
  - mobile h1 高度约 93px，未溢出视口宽度
  - mobile menu opens with four nav items and opaque warm background
  - desktop/mobile console errors: 0
