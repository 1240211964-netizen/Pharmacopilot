# 修改日志

- 将首页导航从上一版五项导航收敛为四项：`课程设计 / 教学实践 / 教学资产 / 评价证据`，移除 `首页` 导航项。
- 将首页 active 状态映射到 `课程设计`，使无“首页”导航项时仍有明确当前态。
- 重写首页导航桌面布局为三段式：左品牌、中间导航组、右操作按钮。
- 将导航容器调整为居中悬浮胶囊：`max-width 1240px`、左右 `24px`、高度 `68px`、顶部 `14px` 呼吸感。
- 按要求使用暖色 tokens：米白玻璃背景、深棕文字、暖灰边框、琥珀 active pill、棕色主按钮。
- 移动端保留菜单按钮，展开面板包含四个导航项和两个操作入口。
- 项目存在全站性能保护规则会禁用所有 `backdrop-filter`；本轮仅对首页导航容器和移动菜单做精确 `blur(18px)` 覆盖，没有恢复全站毛玻璃。
- 保存桌面与移动端浏览器截图和结构检查 JSON。
