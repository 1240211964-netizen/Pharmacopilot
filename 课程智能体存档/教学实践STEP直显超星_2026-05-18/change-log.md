# Change Log

- `前端核心/practice.html`
  - 将超星面板标题改为 `超星 AI 学情分析`。
  - 将主说明改为“点击任一教学 STEP 后直接显示超星页面”。
  - 将原 `嵌入 AI 学情分析` 按钮改为 `刷新当前 STEP`，保留新窗口兜底。
  - 将状态区 `接入方式` 改为 `显示方式：STEP 点击后自动显示`。

- `前端核心/app.js`
  - STEP 节点的可访问文案改为“点击后直接显示超星页面”。
  - 点击 STEP 后自动触发超星显示；若页面已经打开，再点其他 STEP 会直接切换当前环节状态，不再卡在加载中。
  - `刷新当前 STEP` 会强制重新加载当前超星页面。
  - 去掉面向用户的 `iframe` / `内嵌` 状态文案。

- `前端核心/agent-runtime/action-engine.js`
  - 同步动态渲染节点的可访问文案，确保 Copilot 重绘后的 STEP 仍是直接显示超星的入口。

- `前端核心/styles.css`
  - 补充刷新按钮禁用态样式。
