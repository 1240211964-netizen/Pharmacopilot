# 2026-05-08 18:48 Isometric Game Map

## Purpose

Upgrade the current training map from a flat island layout to a stronger pseudo-3D strategy-game scene while preserving existing training state, step data, scoring, and the map detail panel.

## Pre-change Backup

`change-records/backups/2026-05-08-1848-isometric-game-map/`

Backed up before edits:

- `前端核心/navigation.html`
- `前端核心/app.js`
- `前端核心/styles.css`
- Existing generated counterparts under `dist/` and `dist/launch/` when present

## Files Changed

- `前端核心/navigation.html`
- `前端核心/app.js`
- `前端核心/styles.css`
- Generated by `npm run build:static`:
  - `dist/navigation.html`
  - `dist/app.js`
  - `dist/styles.css`
  - `dist/launch/navigation.html`
  - `dist/launch/app.js`
  - `dist/launch/styles.css`

## Changes

- Updated navigation CSS/JS cache query strings to `isometric-map-20260508`.
- Adjusted the SVG route lines to read more like stone-road and bridge paths between islands.
- Added scene structure inside each stage island:
  - `game-island-cliff`
  - `game-island-scenery`
  - two island roads per stage
  - stage-specific scenery props
- Added `renderGameIslandScenery()` with 32 generated scenery props across the 4 islands.
- Added a final isometric-scene CSS layer:
  - stronger water/fog board background
  - heavier island depth with stacked cliff shadows
  - terrain-specific cliff colors
  - stone-road rings on each island
  - miniature towers, ruins, trees, rocks, docks, and flags
  - raised game-style node medallions
  - stronger orange glow for the current node
  - darker bottom route legend
- Added containment rules so the 3D islands do not visually collide with side panels.
- Added a mid-width responsive layout: the detail/assets panel moves below the map so the map remains wide and readable.

## Verification

- `node --check 前端核心/app.js`
- `npm run build:static`
- `node --check dist/app.js`
- `node --check dist/launch/app.js`
- Search check over `前端核心` and `dist`: legacy prohibited wording count is `0`.
- Browser check on `http://127.0.0.1:5182/navigation.html`:
  - 4 stage islands rendered.
  - 20 map nodes rendered.
  - 4 cliff layers rendered.
  - 32 scenery props rendered.
  - 8 island road elements rendered.
  - Step detail panel rendered.
  - 3 unlocked-asset cards rendered.
  - Clicking an unlocked node updates the active node and detail panel.
  - Locked-node click still shows `请先完成前序关卡，再解锁该环节`.
- Visual screenshot check confirmed the map now reads as a larger pseudo-3D scene with raised terrain, roads, props, and stronger node medallions.
