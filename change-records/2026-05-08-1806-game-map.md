# 2026-05-08 18:06 Game Map

## Purpose

Apply `/Users/yandilei/Downloads/CODEX_PROMPT_GAME_MAP.md`: convert the teaching navigation map into a restrained strategy-game-style 20-step learning map while preserving the existing training workflow and state logic.

## Pre-change Backup

`change-records/backups/2026-05-08-1806-game-map/`

Backed up before edits:

- `前端核心/navigation.html`
- `前端核心/app.js`
- `前端核心/styles.css`
- Existing generated counterparts under `dist/` and `dist/launch/` when present

## Files Changed

- `前端核心/navigation.html`
- `前端核心/app.js`
- `前端核心/styles.css`
- Generated by `npm run build:static`:
  - `dist/navigation.html`
  - `dist/app.js`
  - `dist/styles.css`
  - `dist/launch/navigation.html`
  - `dist/launch/app.js`
  - `dist/launch/styles.css`

## Changes

- Replaced the navigation-page map module with `game-map-section` while preserving the Hero, three-step path, Goal Calibrator, 20-step training workspace, and report area.
- Removed the right-side diagnosis panel from the map module; kept a lightweight unlocked-assets panel.
- Added `GAME_TRAINING_MAP_STAGE_META`, `renderGameTrainingMap()`, and `renderGameMapAssets()` in `app.js`.
- Updated `renderTrainingWorkflow()` to refresh the game map from the existing training state.
- Kept `renderTrainingProgressMap()` and `renderTrainingMapAssets()` as compatibility wrappers.
- Preserved existing state sources and behavior:
  - `trainingState.currentStepId`
  - `trainingState.completedStepIds`
  - `trainingState.needsReviewStepIds`
  - `getContextualTrainingWorkflowConfig()`
  - `getTrainingStepStatus()`
  - `getTrainingSidebarScoreMeta()`
- Locked map nodes now keep their click handler and show the existing toast instead of being semantically disabled.
- Added strategy-game-style map CSS: four floating islands, terrain colors, water/mist board, route lines, current-node orange glow, status colors, and responsive single-column behavior.
- Adjusted node labels to avoid visual overlap: the map uses numbered nodes by default and shows the current node label while preserving `title` and `aria-label`.
- Added a cache-busting query string to the navigation page's local CSS/JS references so the updated map assets load reliably.

## Verification

- `node --check 前端核心/app.js`
- `npm run build:static`
- `node --check dist/app.js`
- `node --check dist/launch/app.js`
- Search check: no `trainingMapDiagnosisList`, `trainingMapPriority`, or `教学诊断` remains in `前端核心` or `dist`.
- Search check: no legacy prohibited wording remains in `前端核心` or `dist`.
- Browser check on `http://127.0.0.1:5180/navigation.html`:
  - 4 stage islands rendered.
  - 20 map nodes rendered.
  - 3 unlocked-asset cards rendered.
  - Diagnosis panel count is 0.
  - Exactly 1 current node rendered.
  - Locked-node click shows `请先完成前序关卡，再解锁该环节`.
  - Unlocked-node click updates the current node and re-renders the workflow.
  - Non-current persistent label count is 0, avoiding static label overlap.
